# ── Backend ─────────────────────────────────────────────────────
APP_ID=                   # Application ID
APP_SECRET=               # Application secret (used for JWT signing)

# ── Database ───────────────────────────────────────────────────
DATABASE_URL=             # MySQL connection string (mysql://user:pass@host:port/db)
DEMO_MODE=false           # Set true only for public demo deployments without a persistent database

# ── Frontend (exposed to browser via Vite) ──────────────────────
VITE_APP_NAME="Gram Panchayat Management System"

# ── Admin Role ──────────────────────────────────────────────────
OWNER_UNION_ID=           # Union ID of the app creator; this user gets role "admin" on first login
ADMIN_EMAIL=              # Initial administrator email for npm run db:seed-admin
ADMIN_PASSWORD=           # Initial administrator password, minimum 12 characters
ADMIN_NAME=               # Initial administrator display name

# ── WhatsApp Cloud API Complaint Intake ────────────────────────
WHATSAPP_VERIFY_TOKEN=    # Any strong random string; enter the same value in Meta webhook verification
WHATSAPP_ACCESS_TOKEN=    # Permanent WhatsApp Cloud API access token
WHATSAPP_PHONE_NUMBER_ID= # Phone number ID from Meta WhatsApp Business settings
WHATSAPP_API_VERSION=v21.0
